#!/bin/bash
# can be used for performing login, takes two inputs  - enters in first editText and presses enter key and then enters in second edit text and then presses enter again 
adb shell input text $1 && adb shell input keyevent 66 && adb shell input text $2 && adb shell input keyevent 66 
